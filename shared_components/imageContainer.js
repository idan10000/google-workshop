import {Image, Text, View} from "react-native";
import {Nofar_styles} from "../screens/utils/Nofar_style";
import React from "react";

export default function imageContainer({data}) {
    return (
        <View>

        </View>

    );
}
