class Report{

    constructor(image, location, tagList, description) {
        self.image = image;
        self.loc = location
        self.tagList = tagList
        self.description = description
    }
}
